{
  "name": "better-git-vscode",
  "displayName": "Better Git VS Code",
  "description": "Fast, keyboard-driven Git review for the agentic age.",
  "version": "1.2.36",
  "icon": "src/icon.png",
  "engines": {
    "vscode": "^1.83.0"
  },
  "keywords": [
    "next",
    "change",
    "changed",
    "file",
    "files",
    "shortcut",
    "hotkey",
    "git",
    "agentic",
    "ai",
    "review",
    "keyboard-driven",
    "scm",
    "source control",
    "cycle",
    "previous"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/EthanSK/better-git-vscode"
  },
  "publisher": "EthanSK",
  "categories": [
    "SCM Providers"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "better-git-vscode.next-scm-change",
        "category": "Better Git",
        "title": "Next git change"
      },
      {
        "command": "better-git-vscode.previous-scm-change",
        "category": "Better Git",
        "title": "Previous git change"
      },
      {
        "command": "better-git-vscode.next-changed-file",
        "category": "Better Git",
        "title": "Next changed file"
      },
      {
        "command": "better-git-vscode.previous-changed-file",
        "category": "Better Git",
        "title": "Previous changed file"
      },
      {
        "command": "better-git-vscode.revert-and-save",
        "category": "Better Git",
        "title": "Revert selected changes and save"
      },
      {
        "command": "better-git-vscode.stage-and-next-changed-file",
        "category": "Better Git",
        "title": "Stage current file and advance to next unstaged change"
      },
      {
        "command": "better-git-vscode.stage-and-previous-changed-file",
        "category": "Better Git",
        "title": "Stage current file and advance to previous unstaged change"
      },
      {
        "command": "better-git-vscode.stage-current-file",
        "category": "Better Git",
        "title": "Stage current file",
        "icon": "$(add)"
      },
      {
        "command": "better-git-vscode.stage-current-file-and-advance",
        "category": "Better Git",
        "title": "Stage current file and advance in last-navigated direction",
        "icon": "$(add)"
      },
      {
        "command": "better-git-vscode.smart-forward",
        "category": "Better Git",
        "title": "Smart forward (next change in a diff, else navigate forward)"
      },
      {
        "command": "better-git-vscode.smart-back",
        "category": "Better Git",
        "title": "Smart back (previous change in a diff, else navigate back)"
      },
      {
        "command": "better-git-vscode.reveal-current-file-in-explorer",
        "category": "Better Git",
        "title": "Open & Reveal File in Explorer"
      },
      {
        "command": "better-git-vscode.open-change-at-position",
        "category": "Better Git",
        "title": "Open changes (diff) at current cursor/scroll position"
      },
      {
        "command": "better-git-vscode.reveal-last-staged-file",
        "category": "Better Git",
        "title": "Reopen the diff of the last file staged via Better Git"
      },
      {
        "command": "better-git-vscode.collapse-worktrees",
        "category": "Better Git",
        "title": "Collapse all worktree / repository sections in Source Control"
      },
      {
        "command": "better-git-vscode.add-current-worktree-to-workspace",
        "category": "Better Git",
        "title": "Add current file's git worktree to workspace"
      },
      {
        "command": "better-git-vscode.open-index-in-system-browser",
        "category": "Better Git",
        "title": "Open index.html in System Browser"
      },
      {
        "command": "better-git-vscode.copy-worktree-name",
        "category": "Better Git",
        "title": "Copy Worktree Name"
      },
      {
        "command": "better-git-vscode.add-worktree-to-workspace",
        "category": "Better Git",
        "title": "Add Worktree to Workspace"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "better-git-vscode.stage-current-file-and-advance",
          "_comment_editorTitleOrder": "Keep the + at navigation@100. VS Code's right-aligned editor-title navigation group sorts actions by ascending order, so the highest order is farthest right: @100 gives + the stable far-right hover spot promised in v1.2.16, while VS Code's built-in diff Previous/Next Change arrows at orders 10/11 stay to its LEFT. The v1.2.19 attempt to use @9 was a regression because it swapped + to the other side of those arrows, producing the user-reported 'inverse buttons' order and breaking that muscle memory. On crowded diffs, + may deliberately overflow into the ... menu at @100; v1.2.20's always-visible status-bar Stage & Next button now handles that case as the overflow-proof fallback, so preserving the stable title-bar position is the intentional tradeoff.",
          "group": "navigation@100",
          "when": "gitOpenRepositoryCount != 0"
        }
      ],
      "commandPalette": [
        {
          "command": "better-git-vscode.add-current-worktree-to-workspace",
          "when": "gitOpenRepositoryCount != 0"
        },
        {
          "command": "better-git-vscode.open-index-in-system-browser",
          "when": "false"
        },
        {
          "command": "better-git-vscode.copy-worktree-name",
          "when": "false"
        },
        {
          "command": "better-git-vscode.add-worktree-to-workspace",
          "when": "false"
        }
      ],
      "scm/resourceState/context": [
        {
          "command": "better-git-vscode.open-index-in-system-browser",
          "group": "navigation@-1000",
          "when": "scmProvider == git"
        },
        {
          "command": "better-git-vscode.reveal-current-file-in-explorer",
          "group": "navigation@-999",
          "when": "scmProvider == git"
        }
      ],
      "explorer/context": [
        {
          "command": "better-git-vscode.open-index-in-system-browser",
          "group": "navigation@-1000",
          "when": "resourceScheme == file && resourceFilename == 'index.html'"
        }
      ],
      "scm/sourceControl": [
        {
          "command": "better-git-vscode.copy-worktree-name",
          "group": "navigation@-1000",
          "when": "scmProvider == git && scmProviderContext == worktree"
        },
        {
          "command": "better-git-vscode.add-worktree-to-workspace",
          "group": "navigation@-999",
          "when": "scmProvider == git && scmProviderContext == worktree"
        }
      ]
    },
    "configuration": {
      "title": "Better Git VS Code",
      "properties": {
        "better-git-vscode.treeView": {
          "type": "boolean",
          "default": false,
          "description": "Check this If you use Tree view (as opposed to List view)"
        },
        "better-git-vscode.shouldOpenScmView": {
          "type": "boolean",
          "default": true,
          "description": "Controls whether the source control side panel should open when going to next/previous change"
        },
        "better-git-vscode.revealStagedInSourceControl": {
          "type": "boolean",
          "default": false,
          "description": "Experimental: also select/highlight staged files in the Source Control view while navigating (works around a VS Code limitation where staged diffs can't be auto-revealed). May cause a brief flash of the file, and for partially-staged files highlights the unstaged copy. Requires scm.autoReveal (on by default)."
        },
        "better-git-vscode.autoAddWorktreeOnReveal": {
          "type": "boolean",
          "default": true,
          "description": "Automatically add a file's git worktree as a workspace folder when Open & reveal current file in Explorer cannot reveal it because that worktree is outside the current Explorer workspace. Turn this off to open the editable file without changing workspace folders."
        },
        "better-git-vscode.currentFileBadge": {
          "type": "string",
          "default": "🔥🔥",
          "description": "The badge shown on the file you're currently reviewing, in the Source Control panel (and Explorer/tabs). Max 2 characters — an emoji renders in colour and is the most visible option (the Source Control panel ignores decoration colours). Default is double fire 🔥🔥. Loud choices: 🔥 🔴 🚨 🟥 ❗ ⭐ 👉. Tip: two emoji (e.g. 🔥🔥) is allowed and renders a bit wider. Set to empty to disable the badge."
        },
        "better-git-vscode.dvorakMode": {
          "type": "boolean",
          "default": false,
          "description": "Keep navigation on the same PHYSICAL '>' and '<' keys under a Dvorak layout: next/previous change become alt+v / alt+w (and stage-and-advance shift+alt+v / shift+alt+w) — the characters a Dvorak keyboard types on the keys where QWERTY has '.' and ','. Same commands and behaviour as the QWERTY alt+. / alt+, defaults, just remapped characters. Your own keybindings.json entries always take precedence."
        },
        "better-git-vscode.showLastStagedInStatusBar": {
          "type": "boolean",
          "default": true,
          "description": "Show a status bar item (bottom-left) with the last file you staged through this extension (stage-and-advance or the editor-title + button). Useful when stage-and-advance jumps so fast you don't notice what got staged — click the item to reopen that file's staged diff and unstage it if it was a mistake. Turn off to hide the indicator."
        },
        "better-git-vscode.showStageAndAdvanceInStatusBar": {
          "type": "boolean",
          "default": true,
          "description": "Show a persistent '+ Stage & Next' button in the bottom-left status bar. Unlike editor-title actions, its position does not change when VS Code adds or removes file-type-specific diff buttons. Clicking it stages the current changed file and advances in the last navigation direction. Turn off to keep only the editor-title + button."
        },
        "better-git-vscode.experimentalScmTreeStateManagement": {
          "type": "boolean",
          "default": false,
          "description": "Experimental master switch for automatic Better Git Source Control tree-state management. OFF by default. When off, Better Git never reveals or focuses Source Control, selects or walks SCM rows, or expands/collapses SCM tree nodes during startup; vanilla VS Code controls the tree. The manual Collapse all worktree / repository sections command remains available and runs only when explicitly invoked. Exact mixed-state restoration is paused because VS Code exposes no reliable per-node API."
        },
        "better-git-vscode.collapseWorktreesOnStartup": {
          "type": "boolean",
          "default": false,
          "description": "Experimental double opt-in: after repository discovery settles, reveal Source Control and run VS Code's built-in collapse-all-repositories command exactly once. No retries or row traversal. Has no effect unless better-git-vscode.experimentalScmTreeStateManagement is also enabled."
        },
        "better-git-vscode.newFileNavLineJump": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "For a genuinely NEW file (untracked / staged-new / intent-to-add) shown as a plain editor, the next/previous-change shortcuts have no per-change hunks to jump between — so instead they step the cursor this many lines down/up, letting you scroll through and review the entire new file. When you reach the bottom (or top), they fall through to the next (or previous) changed file as usual. Never affects modified, deleted or renamed files, or any side-by-side diff — those always navigate change-to-change."
        },
        "better-git-vscode.hunkStagingEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Present and step through any hunk whose complete rendered range is not visible, using the same next/previous-change keys instead of letting unread lines run off-screen. A newly reached hidden tail is lifted into view; genuinely larger hunks then scroll by ~a screenful until the exact edge is presented, and only the following press advances. Auto detection follows actual rendered positions, so it adapts to zoom, wrapping, sticky context, diff alignment, and where the hunk landed. Hunks already shown in full are unaffected. Turn this off to always jump hunk-to-hunk."
        },
        "better-git-vscode.hunkStagingThreshold": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Minimum hunk height, in lines, before tall-hunk staging engages. 0 (default) = AUTO: inspect the hunk's actual first and final rendered positions, so staging follows zoom, wrapping, sticky context, diff alignment, and the current landing position. Set a positive number to override with a fixed line count (e.g. 40 = only stage hunks taller than 40 lines). Only applies when Tall-hunk staging is enabled."
        },
        "better-git-vscode.hunkStagingLineStep": {
          "type": "number",
          "default": 5,
          "minimum": 0,
          "description": "How many logical lines each within-hunk step moves while stepping through a tall hunk. Defaults to 5. Set any positive number for an exact custom step, or 0 for AUTO: one viewport height minus the overlap. Only applies when Tall-hunk staging is enabled."
        },
        "better-git-vscode.hunkStagingOverlap": {
          "type": "number",
          "default": 4,
          "minimum": 0,
          "description": "How many lines of overlap to keep on screen between consecutive tall-hunk steps, so reading context carries across each step (a blind full-page jump loses your place). Default 4. Used by the AUTO step size (viewport minus this overlap) and to decide when the remaining tail of a hunk is too small to bother stepping (it just advances to the next hunk instead). Only applies when Tall-hunk staging is enabled."
        },
        "better-git-vscode.debugLogging": {
          "type": "boolean",
          "default": false,
          "description": "Log tall-hunk stepping decisions to the 'Better Git' output channel (View → Output → pick 'Better Git' from the dropdown). Each next/previous-change press logs the direction, viewport height, hunk extent, top/bottom visible lines, caret line, computed target, remaining lines, and the decision taken (stepped / advanced / reached-end). Off by default (no noise); turn on only when diagnosing a stepping issue."
        }
      }
    },
    "_comment_dvorakMode_keybindings": [
      "LAYOUT KEYBINDINGS — the single-source-of-truth architecture (package.json is strict JSON,",
      "so this human-readable explanation lives in this ignored `_comment_*` key; VS Code ignores",
      "any contributes key it doesn't recognise).",
      "",
      "THE CANONICAL BEHAVIOUR lives in exactly TWO commands: `next-scm-change` and `previous-scm-change`",
      "(goToNextDiff / goToPreviousDiff in src/extension.ts — hunk-to-hunk nav, new-file scroll mode,",
      "cross-file fallthrough, all the accumulated fixes). Keyboard layouts do NOT get their own behaviour;",
      "they only change WHICH CHARACTER the same physical key produces. Dvorak mode is therefore a THIN",
      "KEY-REMAP WRAPPER: same commands, different character labels for the same physical keys.",
      "",
      "The design centre is the PHYSICAL `>` and `<` keys (the . and , keycaps — the arrows literally",
      "point forward/back):",
      "  physical > key : QWERTY types `.`  -> alt+.  | Dvorak types `v` -> alt+v  => next-scm-change",
      "  physical < key : QWERTY types `,`  -> alt+,  | Dvorak types `w` -> alt+w  => previous-scm-change",
      "  shift + the same physical key = stage-and-next / stage-and-previous, in BOTH layouts",
      "  (QWERTY shift+alt+. / shift+alt+, ; Dvorak shift+alt+v / shift+alt+w).",
      "The gate is VS Code's native `config.<settingId>` when-clause support on the boolean setting",
      "`better-git-vscode.dvorakMode` — `when: config...dvorakMode` enables the Dvorak-character entry,",
      "`!config...dvorakMode` enables the QWERTY-character entry. Exactly one entry per command is live at",
      "a time; toggling the setting swaps them with no restart. User keybindings.json entries still win.",
      "",
      "HISTORY OF THE v1.2.5 FIX (why next/previous-scm-change moved from alt+z/alt+a to alt+./alt+,):",
      "Before v1.2.5 the QWERTY >/< keys (alt+./alt+,) were bound to smart-forward/smart-back instead.",
      "Those are Ethan's MOUSE-button commands (Karabiner F13/F17), and commit 6043d05 intentionally",
      "FLIPPED their in-diff direction for the mouse (forward = PREVIOUS change — his preference for the",
      "thumb buttons). Because the keyboard >/< keys shared those commands, QWERTY keyboards inherited the",
      "mouse flip: pressing > went BACKWARD through changes, < went FORWARD, and outside a diff the keys",
      "did editor history navigation instead of change navigation. Dvorak was immune the whole time —",
      "its physical >/< keys type v/w, which hit next/previous-scm-change directly. That working Dvorak",
      "path is the source of truth; v1.2.5 reverse-maps it back onto QWERTY so both layouts run the",
      "identical canonical commands from the identical physical keys. The old alt+z/alt+a QWERTY defaults",
      "were dropped: alt+z was shadowing VS Code's built-in Toggle Word Wrap (a real conflict — that",
      "default is restored now), and alt+a had no mnemonic; >/< is the advertised, correct home.",
      "",
      "smart-forward/smart-back KEEP their alt+./alt+, defaults ONLY under dvorakMode (where those",
      "characters sit on different physical keys and don't collide with v/w) so Ethan's working Dvorak",
      "setup is preserved byte-for-byte. On QWERTY they ship NO default keyboard binding — they are",
      "mouse-button commands (bind F13/F17 or your own key in keybindings.json if you want them). Do NOT",
      "give them QWERTY defaults on >/< again; that is exactly the bug v1.2.5 fixed.",
      "",
      "Everything else (changed-file nav cmd+alt+,/cmd+alt+. , alt+q revert, alt+r reveal) is LEFT ON ITS",
      "DEFAULT regardless of dvorakMode — those characters are reachable/comfortable on both layouts and",
      "Ethan never remapped them."
    ],
    "_comment_ctrlShiftG_keybinding": [
      "ctrl+shift+g (the LAST entry in `keybindings` below) is a COMPOSITE binding built on VS Code's",
      "built-in `runCommands` command. `runCommands` runs each id in `args.commands` in order, and it IS",
      "supported directly inside contributes.keybindings (the manifest keybindings go through the SAME",
      "resolver as a user keybindings.json, so the args-bearing form works identically here — no extension",
      "code needed). We use it so one chord does two things in sequence:",
      "  1. better-git-vscode.open-change-at-position — open the active file's side-by-side diff scrolled to",
      "     the EXACT cursor + top line you were viewing in the plain file (our new mirror-of-reveal command).",
      "  2. workbench.view.scm — then focus/open the Source Control pane so the file is highlighted there.",
      "WHY ctrl+shift+g: it shadows VS Code's default 'show Source Control' chord, which only did step 2 —",
      "this upgrades it to 'jump into this file's changes at my position AND show Source Control'. We bind the",
      "SAME key on mac + win/linux (ctrl, not cmd) on purpose: cmd+shift+g is taken on mac (find-next-match),",
      "so ctrl+shift+g is the cross-platform-consistent choice. Users can override/disable it in their own",
      "keybindings.json exactly like every other default (a `-runCommands` minus-entry on ctrl+shift+g)."
    ],
    "keybindings": [
      {
        "command": "better-git-vscode.next-scm-change",
        "key": "alt+.",
        "mac": "alt+.",
        "when": "!config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.next-scm-change",
        "key": "alt+v",
        "mac": "alt+v",
        "when": "config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.previous-scm-change",
        "key": "alt+,",
        "mac": "alt+,",
        "when": "!config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.previous-scm-change",
        "key": "alt+w",
        "mac": "alt+w",
        "when": "config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.smart-forward",
        "key": "alt+.",
        "mac": "alt+.",
        "when": "config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.smart-back",
        "key": "alt+,",
        "mac": "alt+,",
        "when": "config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.next-changed-file",
        "key": "ctrl+alt+.",
        "mac": "cmd+alt+."
      },
      {
        "command": "better-git-vscode.previous-changed-file",
        "key": "ctrl+alt+,",
        "mac": "cmd+alt+,"
      },
      {
        "command": "better-git-vscode.stage-and-next-changed-file",
        "key": "shift+alt+.",
        "mac": "shift+alt+.",
        "when": "!config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.stage-and-next-changed-file",
        "key": "shift+alt+v",
        "mac": "shift+alt+v",
        "when": "config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.stage-and-previous-changed-file",
        "key": "shift+alt+,",
        "mac": "shift+alt+,",
        "when": "!config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.stage-and-previous-changed-file",
        "key": "shift+alt+w",
        "mac": "shift+alt+w",
        "when": "config.better-git-vscode.dvorakMode"
      },
      {
        "command": "better-git-vscode.revert-and-save",
        "key": "alt+q",
        "mac": "alt+q"
      },
      {
        "command": "better-git-vscode.reveal-current-file-in-explorer",
        "key": "alt+r",
        "mac": "alt+r"
      },
      {
        "command": "runCommands",
        "key": "ctrl+shift+g",
        "mac": "ctrl+shift+g",
        "args": {
          "commands": [
            "better-git-vscode.open-change-at-position",
            "workbench.view.scm"
          ]
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "test:index-context-menu": "node scripts/test-index-context-menu.mjs",
    "test:scm-state": "npm run test:scm-state:disabled && npm run test:scm-state:enabled-inert && npm run test:scm-state:collapse",
    "test:scm-state:disabled": "node scripts/test-scm-state-persistence.mjs --expect-disabled",
    "test:scm-state:enabled-inert": "node scripts/test-scm-state-persistence.mjs --expect-enabled-inert",
    "test:scm-state:collapse": "node scripts/test-scm-state-persistence.mjs --expect-collapse"
  },
  "devDependencies": {
    "@types/vscode": "^1.83.0",
    "@types/mocha": "^10.0.3",
    "@types/node": "18.x",
    "@typescript-eslint/eslint-plugin": "^6.9.0",
    "@typescript-eslint/parser": "^6.9.0",
    "eslint": "^8.52.0",
    "glob": "^10.3.10",
    "mocha": "^10.2.0",
    "typescript": "^5.2.2",
    "ts-loader": "^9.5.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "@vscode/test-electron": "^2.3.6"
  }
}
